{
  "name": "hanshi",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test --stream",
    "lint": "lerna run lint",
    "format": "lerna run format"
  }
}
