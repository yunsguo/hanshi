const functor: unique symbol = Symbol.for('Typeclass.Functor');

const applicable: unique symbol = Symbol.for('Typeclass.Applicable');

const monad: unique symbol = Symbol.for('Typeclass.Monad');

const decay: unique symbol = Symbol.for('Typeclass.Decay');

export { functor, applicable, monad, decay };
